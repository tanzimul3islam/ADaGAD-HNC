name: tuned
max_epochs: 250
batch_size: 1
lr: 0.0005
weight_decay: 1e-4
lr_scheduler: cosine
early_stopping_patience: 50
gradient_clip_norm: 1.0
val_every: 5
save_every: 10
amp: false
checkpoint_dir: outputs
log_every: 1
